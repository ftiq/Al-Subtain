<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_server_test_fix" model="ir.actions.server">
        <field name="name">🔧 اختبار إصلاح التقدم</field>
        <field name="model_id" ref="model_lab_result_set"/>
        <field name="binding_model_id" ref="model_lab_result_set"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">

for record in records:
    record._compute_statistics()
    record.message_post(
        body=f"تم تحديث التقدم: {record.progress_percentage:.2%}",
        message_type='notification'
    )

raise UserError("✅ تم الاختبار بنجاح")
        </field>
    </record>
</odoo> 