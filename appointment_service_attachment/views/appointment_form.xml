
<odoo>
  <template id="custom_appointment_form" name="Custom Appointment Form">
    <t t-call="website.layout">
      <div class="container">
        <h2>Book an Appointment</h2>
        <form action="/appointment/custom_submit" method="post" enctype="multipart/form-data">
          <div class="form-group">
            <label for="name">Your Name</label>
            <input type="text" name="name" class="form-control" required="1"/>
          </div>
          <div class="form-group">
            <label for="service_product_id">Select a Service</label>
            <select name="service_product_id" class="form-control" required="1">
              <option value="">-- Choose a Service --</option>
              <t t-foreach="services" t-as="product">
                <option t-att-value="product.id"><t t-esc="product.display_name"/></option>
              </t>
            </select>
          </div>
          <div class="form-group">
            <label for="attachment_file">Upload File (optional)</label>
            <input type="file" name="attachment_file" class="form-control"/>
          </div>
          <div class="form-group mt-3">
            <button type="submit" class="btn btn-primary">Confirm Appointment</button>
          </div>
        </form>
      </div>
    </t>
  </template>
</odoo>
